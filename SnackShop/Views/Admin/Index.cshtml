@model List<SnackShop.Core.Models.ProductModel>
@{
    ViewData["Title"] = "Index";
}

<h2>Snacks</h2>

<ul>
    @foreach (var product in @Model)
    {
        <li><a href="/admin/edit/@product.Slug">@product.Name</a></li>
    }
</ul>

<a href="/admin/add" class="btn btn-primary">Add Snack</a>

